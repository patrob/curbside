node_modules/
**/dist/
**/*.tsbuildinfo

# Secrets — never commit. Cookie jars carry a live logged-in session.
.cache/
*.cookies.json
cookies.json

# Debug dumps captured when a response fails to parse.
.debug/

# OS
.DS_Store
